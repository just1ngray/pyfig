configs:
  - some/path/to/config.yaml

evaluators:
  pyfig.VariableEvaluator:
    camera_id: "DEV00X"
    environment: "reeldata-config"
  pyfig.EnvironmentEvaluator: {}

overrides:
  reelbiomass_common:
    mqtt:
      port: 1234
